<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required on Android 13+ (API 33) for watering-reminder local
         notifications to be requestable at all; permission_handler's
         Permission.notification check/request (see NotificationSettingsScreen)
         is a no-op without this manifest entry. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Coarse is enough to resolve a season/weather region for Care
         Calculators (see CalculatorsScreen) — fine location isn't needed. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for PermissionsScreen/permission_handler's Permission.camera
         request to show the OS prompt at all — without this declaration,
         Permission.camera.request() silently resolves to denied with no
         dialog. image_picker's ImageSource.camera itself delegates to the
         system camera app via an implicit intent and doesn't strictly need
         this, but our own explicit pre-flight request (see
         PermissionsScreen._handleAllow and settings_subscreens.dart's camera
         status check) does. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:label="VANYA"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:enableOnBackInvokedCallback="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Home-screen "watering" widget — see WateringWidgetProvider.kt.
             android:exported="true" is required for widget receivers (the
             OS itself broadcasts APPWIDGET_UPDATE to it), not a
             third-party-facing surface. -->
        <receiver
            android:name=".WateringWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/watering_widget_info" />
        </receiver>
        <!-- home_widget's own receiver for Dart-side background callbacks —
             not used by WateringWidgetProvider (it only reads data the app
             already pushed, no background Dart work needed), but the
             plugin expects this registered regardless. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
